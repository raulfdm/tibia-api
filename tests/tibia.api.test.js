/* const TibiaAPI = require('./tibia.api');

test('validateParameter equals null', () => {
    return TibiaAPI
        .validateParameter()
        .catch(err => expect(err).toThrow(Error))
})
test('validateParameter with some number', () => {
    let param = "raul1";
    return TibiaAPI
        .validateParameter(param)
        .catch(err => expect(err).toThrow())
}) */

test('',()=>{})