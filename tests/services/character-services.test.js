//const { getCharacterPage } = require('./character-services')

const jsdom = require('jsdom')
const { JSDOM } = jsdom

jest.setTimeout(10000)

describe('TESTING getCharacterPage method', () => {
  /*  test(`should throw when doesn't pass character Name`, async () => {
    expect.assertions(1)
    try {
      await getCharacterPage()
    } catch (error) {
      expect(error).toThrow()
    }
  }) */
/* 
  test('Should contain a Name input at returned DOM', async done => {
    expect.assertions(1)

    getCharacterPage('mad dentist').then(dom => {
      expect(dom).toBeInstanceOf(JSDOM)
    })
  }) */
  test('Should ',()=>{

  })
})
